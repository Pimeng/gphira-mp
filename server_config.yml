# Phira-MP 服务器配置

# 服务器监听地址（默认0.0.0.0，监听所有网卡）
host: "0.0.0.0"

# 服务器监听端口（默认12346）
port: 12346

# HTTP服务配置
# 启用HTTP服务（用于管理API和回放功能）
http_service: false

# HTTP服务端口（默认12347）
http_port: 12347

# 管理员Token（用于访问管理API）
# 如果不设置，可以通过OTP方式获取临时Token
# admin_token: "your_secure_token_here"

# 直播模式: 是否启用实时数据传输（触摸帧和判定事件）
# 启用后，允许观察的用户可以实时观看游戏画面
live_mode: false

# 允许观察的用户ID列表（仅在直播模式启用时生效）
monitors:
  - 2

# 日志级别: debug, info (默认), warn, error
# debug: 输出所有日志，包括心跳包
# info: 只输出重要事件（连接、断开、房间操作等）
log_level: info

# 房间默认最大玩家数（1-64，默认12）
default_max_users: 12

# 管理员数据文件路径（封禁数据等）
# 默认使用 PHIRA_MP_HOME 环境变量或工作目录下的 admin_data.json
# admin_data_path: "/path/to/admin_data.json"

# 启用HAProxy PROXY Protocol支持
tcp_proxy_protocol: false
# HTTP真实IP头，如 X-Forwarded-For, X-Real-IP
real_ip_header: ""
# Phira-MP 服务器配置

# 直播模式: 是否启用实时数据传输（触摸帧和判定事件）
# 启用后，允许观察的用户可以实时观看游戏画面
live_mode: false

# 允许观察的用户ID列表（仅在直播模式启用时生效）
monitors:
  - 2

# 日志级别: debug, info (默认), warn, error
# debug: 输出所有日志，包括心跳包
# info: 只输出重要事件（连接、断开、房间操作等）
log_level: info
